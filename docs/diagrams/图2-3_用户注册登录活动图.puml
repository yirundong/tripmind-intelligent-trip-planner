@startuml 图2-3_用户注册登录活动图
|用户|
start
:访问登录或注册页面;

if (已有账号？) then (是)
  |用户|
  :输入账号与密码;
  |系统|
  :核对账号与密码;
  if (正确？) then (是)
    :签发令牌，识别用户身份;
  else (否)
    :提示账号或密码错误;
    |用户|
    :重新输入账号与密码;
    |系统|
    :核对账号与密码;
    note right: 循环直到正确或放弃
  endif
else (否)
  |用户|
  :填写用户名、邮箱与密码;
  |系统|
  :校验信息合法性与账号是否被占用;
  if (可注册？) then (是)
    :创建账号并自动登录;
  else (否)
    :提示账号已存在或信息无效;
    |用户|
    :重新填写注册信息;
    |系统|
    :校验信息合法性与账号是否被占用;
    note right: 循环直到通过或放弃
  endif
endif

|系统|
:跳转至首页;
|用户|
:进入系统首页;

stop
@enduml
