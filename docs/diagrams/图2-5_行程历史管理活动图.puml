@startuml 图2-5_行程历史管理活动图
|用户|
start
:进入历史行程页面;

|系统|
:识别用户身份;

if (身份有效？) then (否)
  :提示重新登录;
  |用户|
  stop
else (是)
endif

|系统|
:按时间倒序返回历史行程列表;

|用户|
:浏览历史行程列表;

if (选择操作) then (查看详情)
  :点击查看某条行程;
  |系统|
  :展示该条行程的详细安排;
  |用户|
  :查看行程详细安排;
  stop
else (删除)
  :点击删除按钮;
  |系统|
  :弹出删除确认对话框;
  |用户|
  if (确认删除？) then (是)
    |系统|
    :删除该条行程并刷新列表;
    |用户|
    stop
  else (否)
    :返回历史行程列表;
    stop
  endif
endif

@enduml
